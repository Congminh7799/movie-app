import Navbar from "../blocks/Navbar";

const Header = () => {
    return (
        <header className="h-[80px] w-full bg-sky-950 sticky top-0 z-50">
            <Navbar />
        </header>
    )
}

export default Header;